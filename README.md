# Niko.WinForms
A graphical UI library for Windows Forms, which is made in the style of windows from the game OneShot World Machine Edition. / Графическая ui библиотека для Windows Forms, которая сделана в стиле окон из игры OneShot World Mashine Edition

> "You only have one shot to make a beautiful interface."

Компактная и стильная библиотека кастомных компонентов для Windows Forms (C#), полностью воссоздающая цветовую палитру, дух и пиксельную эстетику игры **OneShot**.

---

## Фишки интерфейса

* **Фирменная палитра:** Истинный чёрный фон (`Color.Black`) в сочетании с культовыми оттенками игры (фиолетовый, сине-голубой, янтарно-жёлтый, фосфорный зелёный).
* **Умный NikoThemeManager:** Компонент, который на лету перекрашивает форму и все вложенные элементы.
* **Режим «Радуга» (Rainbow):** Плавный динамический перелив цветов в реальном времени.
* **Безопасность для дизайнера:** Код оптимизирован так, чтобы ничего не лагало и не блокировало Toolbox в режиме конструктора Visual Studio.
* **Пиксельная точность:** Никакого размытия (Anti-aliasing отключен ради сохранения ретро-стиля) и защита от мерцания через двойную буферизацию.

---

## Быстрый старт

Библиотека компилируется сразу под несколько версий `.NET Framework` (от 4.5 до 4.8.1), обеспечивая полную обратную совместимость.

### Установка через менеджер пакетов:
| Bash | NuGet |
|-------|--------|
| ```Install-Package Niko.WinForms ``` | ``` dotnet add package Niko.WinForms ``` |

### Использование
* 1. Откройте скрипт вашей формы и вместо ```YourForm : Form``` пишем ```YourForm : NikoForm```
Пример
``` csharp
using Niko.WinForm

public partial class YourForm : NikoForm
{
    public YourForm()
    {
        InitializeComponent();
    }
}
```
### Элементы

| Элемент | Описание | *Придуманное |
|-----------|-------------|-----------------|
| ```NikoForm``` | Базовая форма с кастомными границами и поддержкой акцентных цветов. | Это часть библиотеки |
| ```NikoThemeManager``` | Компонент для моментальной смены палитры всех элементов формы (включая Context Menu). | Тоже часть библиотеки |
| ```NikoButton``` | Пиксельная кнопка с состояниями Hover Pressed. | Нет |
| ```NikoRadioButton``` | Радио-кнопка | Да |
| ```NikoCheckBox``` | Кастомный чекбокс в стиле интерфейса OneShot. | Да |
| ```NikoComboBox``` | Выпадающий список с полной ручной перерисовкой элементов. | Да |
| ```NikoTextBox``` & ```NikoRichTextBox``` | Текстовые поля с плавной подсветкой рамок при фокусе. | Да |
| ```NikoProgressBar``` | Плоский стилизованный индикатор выполнения. | Да |
| ```NikoSlider``` | Кастомный ползунок для регулировки значений. | Нет |
| ```NikoSliderButton``` | Кнопки от ползунка | Да |
| ```NikoVScroll``` & ```NikoHScroll``` | Скроллбары для интеграции в кастомные панели. | Нет |
| ```NikoContextMenuStrip``` | Выпадающие контекстные меню с поддержкой глобальной темы. | Да |

>  *Подразумевается были ли придуманы или взяты из OneShot World Mashine Edition

### Enum
* ```NikoTheme```

| Элемент | Значение |
|-----------|-------------|
| ```Purple``` | Фиолетовый цвет |
| ```Blue``` | Синий цвет |
| ```Teal``` | Бирюзовой цвет |
| ```Yellow``` | Жёлтый цвет |
| ```White``` | Белый цвет |
| ```Red``` | Красный цвет |
| ```Pink``` | Розовый цвет |
| ```Orange``` | Оранжевый цвет |
| ```Green``` | Зелёный цвет |
| ```Rainbow``` | Переливание всеми цветами |
| ```Custom``` | Кастомный цвет ```Accent Color``` в ```NikoThemeManager``` |

### Доступные методы

| Метод | Значение |
|-----------|-------------|
| ```UpdateEverythin()``` в ```NikoThemeManager``` | Обновить цвет |

### Шрифты
> К сожалению в библиотеку не идут необходимые шрифты, поэтому установка лицензии за использование шрифта лежит на вас

> Вот метод для загрузки кастомного шрифта

```csharp
private static System.Drawing.Text.PrivateFontCollection _nikoFontCollection = new System.Drawing.Text.PrivateFontCollection();

/// <summary>
/// Загружает кастомный шрифт из файла (.ttf/.otf) и возвращает экземпляр Font.
/// Если файл отсутствует, откатывается на стандартный SansSerif.
/// </summary>
public static Font GetCustomFont(string fontPath, float size, FontStyle style = FontStyle.Regular)
{
    try
    {
        if (_nikoFontCollection.Families.Length == 0 && System.IO.File.Exists(fontPath))
        {
            _nikoFontCollection.AddFontFile(fontPath);
        }
        if (_nikoFontCollection.Families.Length > 0)
        {
            return new Font(_nikoFontCollection.Families[0], size, style);
        }
    }
    catch 
    { 
    }
    return new Font(FontFamily.GenericSansSerif, size, style);
}
```
>Из шрифтов вы можете использовать *Terminus TTF* или *Determination Mono / 8-bit Operator* 

## ⚖️ Отказ от ответственности

> **Важно:** Этот проект является неофициальным фанатским инструментом и создан исключительно в некоммерческих, ознакомительных и творческих целях. 

* **Права на интеллектуальную собственность:** Игра **OneShot** и её расширенное издание **OneShot: World Machine Edition (WME)**, включая все связанные товарные знаки, оригинальный визуальный стиль, цветовую палитру, названия и элементы сеттинга, целиком и полностью принадлежат компаниям **Future Cat LLC** и **KOMODO**.
* **Статус репозитория:** Данная библиотека компонентов (`Niko.WinForms`) никак не связана с авторами или издателями оригинальной игры, не спонсируется ими и не поддерживается. Разработчик проекта не претендует на владение чужой интеллектуальной собственностью.

Все права на оригинальные концепты принадлежат их законным правообладателям. Если вам нравится эстетика этого интерфейса, пожалуйста, поддержите разработчиков и приобретите оригинальную игру в Steam или на консолях!
